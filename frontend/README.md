# qTwitch Frontend

Frontend Angular inicial para uma plataforma de streaming de estudo.

## Como rodar

```bash
npm install
npm start
```

A aplicacao roda em `http://localhost:4200` e usa `proxy.conf.json` para encaminhar `/api` ao backend em `http://localhost:8080`.
