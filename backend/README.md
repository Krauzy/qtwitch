# qTwitch Backend

Backend simples em Java Spring Boot para iniciar o estudo de uma plataforma de streaming.

## Como rodar

```bash
mvn spring-boot:run
```

A API sobe em `http://localhost:8080`.

## Endpoints iniciais

- `GET /api/health`
- `GET /api/streams`
- `GET /api/streams/{id}`
